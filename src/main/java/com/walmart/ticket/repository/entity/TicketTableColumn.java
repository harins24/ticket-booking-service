package com.walmart.ticket.repository.entity;

/**
 * Enum that stores columns of tables present in schema
 */
public enum TicketTableColumn {
	LEVEL_ID,
	LEVEL_NAME,
	PRICE,
	NUMBER_OF_ROWS,
	SEATS_IN_ROW
}
